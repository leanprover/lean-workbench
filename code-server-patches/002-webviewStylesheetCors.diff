diff --git a/lib/vscode/src/vs/workbench/contrib/webview/browser/pre/index.html b/lib/vscode/src/vs/workbench/contrib/webview/browser/pre/index.html
index c8e655f..d9104f8 100644
--- a/lib/vscode/src/vs/workbench/contrib/webview/browser/pre/index.html
+++ b/lib/vscode/src/vs/workbench/contrib/webview/browser/pre/index.html
@@ -5,7 +5,7 @@
 	<meta charset="UTF-8">
 
 	<meta http-equiv="Content-Security-Policy"
-		content="default-src 'none'; script-src 'sha256-A6/szVNdTzyi4hDa+9OLbzS8tSd2iUV4CqimLNWex2Y=' 'self'; frame-src 'self'; style-src 'unsafe-inline';">
+		content="default-src 'none'; script-src 'sha256-NBv6SijI4ZAVEU+PVj2Tg7z6WZFBOXndiojufDUUI3c=' 'self'; frame-src 'self'; style-src 'unsafe-inline';">
 
 	<!-- Disable pinch zooming -->
 	<meta name="viewport"
@@ -864,6 +864,22 @@
 				}
 			});
 
+			// Firefox 149-151 service workers do not intercept cross-origin no-cors
+			// stylesheet requests (https://bugzilla.mozilla.org/show_bug.cgi?id=1201160),
+			// so plain stylesheet `<link>`s to the resource authority fail there.
+			// CORS requests are still intercepted, and the service worker responds
+			// with `Access-Control-Allow-Origin: *`, so load stylesheets with CORS.
+			const resourceBaseAuthority = searchParams.get('vscode-resource-base-authority');
+			newDocument.querySelectorAll('link[rel~="stylesheet" i]').forEach(link => {
+				try {
+					if (new URL(link.href).hostname.endsWith('.' + resourceBaseAuthority)) {
+						link.setAttribute('crossorigin', 'anonymous');
+					}
+				} catch (e) {
+					// `link.href` is not a valid URL; leave the element alone
+				}
+			});
+
 			// Set default aria role
 			if (!newDocument.body.hasAttribute('role')) {
 				newDocument.body.setAttribute('role', 'document');
