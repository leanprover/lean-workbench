/** @module Cache tags for Server components. */

/** Invalidated by changes to server configuration. */
export const serverConfig = 'server-config'
/** Invalidated by changes to the `allowedGithubUser` table. */
export const allowedGithubUser = 'allowed-github-user'
