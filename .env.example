# Copy this file to .env.local when developing the web app.
# Copy to .env.docker to set envvars in the Docker container (in `make serve/dev`).

# Required: directory where lean-workbench stores its data (config, database, etc.)
LEAN_WORKBENCH_DATA_DIR=./data